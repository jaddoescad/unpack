# Uncomment the next line to define a global platform for your project
# platform :ios, '10.0'

target 'unpack' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'SnapKit', '~> 5.0.0'
  pod 'Kingfisher', '~> 7.0'
  pod 'Floaty', '~> 4.2.0'
  # Pods for unpack

  target 'unpackTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'unpackUITests' do
    # Pods for testing
  end

end
